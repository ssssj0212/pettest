# 데이터베이스 연결 정보
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/reservation_db

# JWT 시크릿 키 (최소 32자)
# 생성 명령: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=여기에_생성된_랜덤_문자열_붙여넣기

# JWT 토큰 만료 시간 (분)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS 허용 도메인 (쉼표로 구분)
# 개발: http://localhost:3000,http://127.0.0.1:3000
# 프로덕션: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 환경 (development / production)
ENVIRONMENT=development

# 선택적: Google OAuth (백엔드에서도 검증 시)
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

